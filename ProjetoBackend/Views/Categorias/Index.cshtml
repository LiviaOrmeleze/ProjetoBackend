@model IEnumerable<ProjetoBackend.Models.Categoria>

@{
    ViewData["Title"] = "Categorias";
}

<div class="container">


    <h1 class="Titulos">Categorias</h1>

    <form asp-action="Search" method="get">
        <input type="text" name="nome" class="col" placeholder="Buscar cliente por nome..." />
        <button type="submit" class="icone"><ion-icon name="search"></ion-icon></button>
    </form>

    <p>
        <a asp-action="Create" class="btn btn-lg btn-primary btnAdicionar mt-3"><ion-icon name="person-add-outline"></ion-icon>  Adicionar Categoria</a>
    </p>
    <table class="table styled-table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        <a asp-action="Edit" class="btn btnEdd" asp-route-id="@item.CategoriaId">Editar</a>
                        <a asp-action="Details" class="btn btnEdd" asp-route-id="@item.CategoriaId">Detalhes</a>
                        <a asp-action="Delete" class="btn btnEdd" asp-route-id="@item.CategoriaId">Deletar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>